apiVersion: v1
kind: ConfigMap
metadata:
  name: dependency-engine-config
  namespace: kubepilot-system
data:
  SERVICE_NAME: "dependency-engine"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dependency-engine
  namespace: kubepilot-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dependency-engine
  template:
    metadata:
      labels:
        app: dependency-engine
    spec:
      containers:
      - name: dependency-engine
        image: kubepilot-dependency-engine:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: dependency-engine-config
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: dependency-engine
  namespace: kubepilot-system
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: dependency-engine
