apiVersion: v1
kind: ConfigMap
metadata:
  name: recovery-planning-engine-config
  namespace: kubepilot-system
data:
  SERVICE_NAME: "recovery-planning-engine"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recovery-planning-engine
  namespace: kubepilot-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recovery-planning-engine
  template:
    metadata:
      labels:
        app: recovery-planning-engine
    spec:
      containers:
      - name: recovery-planning-engine
        image: kubepilot-recovery-planning-engine:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: recovery-planning-engine-config
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: recovery-planning-engine
  namespace: kubepilot-system
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: recovery-planning-engine
