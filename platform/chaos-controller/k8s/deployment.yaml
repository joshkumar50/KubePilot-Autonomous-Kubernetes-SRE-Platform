apiVersion: apps/v1
kind: Deployment
metadata:
  name: chaos-controller
  namespace: kubepilot-system
  labels:
    app: chaos-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chaos-controller
  template:
    metadata:
      labels:
        app: chaos-controller
    spec:
      containers:
      - name: chaos-controller
        image: kubepilot-chaos-controller:latest
        imagePullPolicy: Never
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true

        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: chaos-controller
  namespace: kubepilot-system
spec:
  selector:
    app: chaos-controller
  ports:
  - port: 80
    targetPort: 8000
