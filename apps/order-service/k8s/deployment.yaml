apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
  namespace: kubepilot-apps
data:
  SERVICE_NAME: "order-service"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: kubepilot-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: kubepilot-order-service:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: order-service-config
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: kubepilot-apps
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: order-service
