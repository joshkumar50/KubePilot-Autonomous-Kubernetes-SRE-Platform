apiVersion: v1
data:
  SERVICE_NAME: kubernetes-controller
kind: ConfigMap
metadata:
  name: kubernetes-controller-config
  namespace: kubepilot-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-controller
  namespace: kubepilot-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-controller
  template:
    metadata:
      labels:
        app: kubernetes-controller
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: kubernetes-controller-config
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        image: kubepilot/kubernetes-controller:latest
        imagePullPolicy: Never
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        name: kubernetes-controller
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
      serviceAccountName: recovery-engine-sa
---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-controller
  namespace: kubepilot-system
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: kubernetes-controller
