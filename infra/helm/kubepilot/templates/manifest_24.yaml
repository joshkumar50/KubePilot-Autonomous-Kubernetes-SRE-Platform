apiVersion: v1
data:
  SERVICE_NAME: policy-engine
kind: ConfigMap
metadata:
  name: policy-engine-config
  namespace: kubepilot-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-engine
  namespace: kubepilot-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: policy-engine
  template:
    metadata:
      labels:
        app: policy-engine
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: policy-engine-config
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        image: kubepilot/policy-engine:latest
        imagePullPolicy: Never
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        name: policy-engine
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
      serviceAccountName: recovery-engine-sa
---
apiVersion: v1
kind: Service
metadata:
  name: policy-engine
  namespace: kubepilot-system
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: policy-engine
