apiVersion: v1
data:
  SERVICE_NAME: root-cause-analysis-engine
kind: ConfigMap
metadata:
  name: root-cause-analysis-engine-config
  namespace: kubepilot-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: root-cause-analysis-engine
  namespace: kubepilot-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: root-cause-analysis-engine
  template:
    metadata:
      labels:
        app: root-cause-analysis-engine
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: root-cause-analysis-engine-config
        - configMapRef:
            name: kubepilot-global-config
        - secretRef:
            name: kubepilot-secrets
        image: kubepilot/root-cause-analysis-engine:latest
        imagePullPolicy: Never
        livenessProbe:
          httpGet:
            path: /liveness
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        name: root-cause-analysis-engine
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: root-cause-analysis-engine
  namespace: kubepilot-system
spec:
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: root-cause-analysis-engine
